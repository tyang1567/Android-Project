<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    app:startDestination="@id/homeFragment">

    <!-- Home Fragment -->
    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.photodiary.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home" />

    <!-- Stream Fragment -->
    <fragment
        android:id="@+id/streamFragment"
        android:name="com.example.photodiary.StreamFragment"
        android:label="Stream"
        tools:layout="@layout/fragment_stream" />

    <!-- Detail Fragment -->
    <fragment
        android:id="@+id/detailFragment"
        android:name="com.example.photodiary.DetailFragment"
        android:label="Detail"
        tools:layout="@layout/fragment_detail" />
</navigation>
